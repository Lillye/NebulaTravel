@model NebulaTravel.ViewModels.Destinations.DestinationsListViewModel

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="icon" type="image/png" href="/images/favicon.png">
    <title>Destinations</title>
    <link rel="stylesheet" type="text/css" href="/styles/spaceships.css" />
</head>
<body>
    <main>
        @Html.Partial("_Header")
        <article>
            @foreach(var destination in Model.Destinations){
                @Html.Partial("_Summary", destination);
            }
            <button id="scrollToTopButton">Up</button>
        </article>
        <div id="space"></div>
    </main>
</body>
@Html.Partial("ViewScripts")
</html>